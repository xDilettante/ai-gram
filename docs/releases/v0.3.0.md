# ai-gram v0.3.0

`v0.3.0` is the first large pre-v1 release for the public `ai-gram` repository. It packages the Bot API 9.6 coverage workstream, public repository polish, CI, MIT licensing, and repository maturity assets into one coherent beta milestone.

This tag already exists locally and on `origin`. Do not retag or force-push `v0.3.0`; use a later version for follow-up Bot API 10.0 work.

## Status

- Release type: pre-v1 / beta.
- Telegram Bot API coverage: code coverage for Bot API 9.6 is complete with documented architecture differences.
- Official method wrappers: 169/169.
- Sensitive and state-changing live smoke remains manual-only.
- Public APIs may still evolve before v1.0.

## Highlights

- Completed Telegram Bot API 9.6 code coverage on `main`.
- Verified all 169 official Bot API 9.6 method wrappers are present.
- Added `setWebhook.certificate` upload support for the official upload-only `InputFile` parameter.
- Added GitHub Actions CI and verified the CI workflow is green on GitHub for the maturity-pack commit.
- Added a local coverage badge and `scripts/update_coverage_badge.sh`.
- Polished the README landing page with a hero image, status badges, concise links, and honest pre-v1 messaging.
- Added the MIT License.
- Added architecture documentation, public examples, issue templates, a PR template, `CONTRIBUTING.md`, and `SECURITY.md`.
- Verified public module installation from GitHub `main` in an external temporary consumer project.

## Bot API 9.6 coverage

The final Bot API 9.6 coverage audit found no known blockers after the `setWebhook.certificate` fix.

The coverage includes typed method wrappers, update/message/service metadata decoding, payments/Stars/gifts, Business APIs, games, Passport, inline/prepared inline support, checklists, reply metadata, ChatFullInfo, and the remaining core update shapes.

Important references:

- [Final Bot API 9.6 coverage audit](../BOT_API_9_6_FINAL_AUDIT.md)
- [API coverage inventory](../API_COVERAGE.md)
- [Architecture notes](../ARCHITECTURE.md)

## Architecture differences

The release keeps several intentional compatibility-oriented differences from the exact wording of the official Telegram Bot API documentation:

- `FileRef` / `FileUpload` instead of a direct official `InputFile` type.
- `GetChat` plus `GetChatFullInfo` compatibility split.
- A flat `ChatMember` compatibility strategy instead of exposing only concrete `ChatMember*` variants.
- `CallbackQuery.Message` plus `MaybeMessage` compatibility strategy for accessible and inaccessible callback messages.
These are documented design choices, not current coverage blockers.

## Install

Install the release with:

```bash
go get github.com/xDilettante/ai-gram@v0.3.0
```

## Verification evidence

Local and public repository checks performed before preparing these notes included:

- GitHub Actions CI success on `main` for commit `f2ab1a55d76b3b4c17498dee5b7dd7c0eb5e93e4`
- public module install check with `go get github.com/xDilettante/ai-gram@main`
- external compile-only consumer snippet using `aigram.NewBot`, `aigram.SendMessageParams`, and `aigram.SendChecklistParams`

## Manual-only live smoke areas

The following areas must not be live-smoked automatically:

- payments, Stars, gifts, and subscription flows;
- Passport data;
- Business API state changes;
- managed bot token methods;
- admin/destructive chat methods;
- sticker set mutation;
- games requiring BotFather setup;
- inline mode flows requiring BotFather/client setup;
- lifecycle methods such as `logOut` and `close`;
- `setWebhook` certificate upload and webhook state changes.

## Follow-up

Bot API 10.0 work belongs to the next release line; see [`v0.4.0`](v0.4.0.md) and the current maintainer release checklist.
