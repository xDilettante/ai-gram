# Live Smoke Matrix

This matrix defines which live checks are safe to run during v0.1 stabilization and which checks require explicit human confirmation. Unit tests and `httptest` coverage remain the default verification path; live smoke is only for integration confidence with real Telegram or a local Telegram Bot API server.

## Safe flows

| Flow | Command/script | Required env | User action | Expected safe logs | Auto/manual | Risk |
| --- | --- | --- | --- | --- | --- | --- |
| Local Bot API smoke | `./scripts/discover_env.sh` then `./scripts/smoke_local_api.sh` | `AIGRAM_CHAT_ID`, local-role token, deploy/botapi SSH target when using remote local Bot API | none | `GetMe`/`GetWebhookInfo` script output without token-bearing URLs | manual script, safe to run when env is configured | Low: read-only API checks. |
| Long polling smoke | `./scripts/smoke_longpoll.sh` | local-role token, `AIGRAM_CHAT_ID`; optional discovered `AIGRAM_BASE_URL` | Open the notified `t.me` link and send one ordinary text message within the wait window | `AIGRAM_SMOKE_UPDATE_RECEIVED`, `matched=message`, `action=send_message ... reply_to_message_id=...`, and `AIGRAM_SMOKE_OK`; no full private text required | manual script plus user action | Low/medium: sends a reply and may call `DeleteWebhook` as documented for long polling mode. |
| Webhook `/start` | `AIGRAM_TARGETED_SMOKE=access ./scripts/deploy_webhook_example.sh` or targeted deploy | webhook-role token, `AIGRAM_WEBHOOK_URL`, deploy env | Open the notified bot deep link and press Start/send `/start` | `action=start_payload`, `action=smoke_keyboard_shown`, `action=send_message` | manual deploy plus user action | Low: sends test messages only. |
| Access panel | `AIGRAM_TARGETED_SMOKE=access ./scripts/deploy_webhook_example.sh`, then `./scripts/remote_logs.sh` | webhook deploy env, admin IDs/chat ID | Open `?start=access_panel`; press `Access status`, optionally `Open access` then `Close access` | `action=access_panel_shown`, `action=access_status`, `action=access_mode_changed mode=public/admin` | manual targeted | Low/medium: `Open access` temporarily allows public access; close it after the check. |
| Chat info flow | access panel in webhook example, then `./scripts/remote_logs.sh` | webhook deploy env, admin IDs/chat ID | Open `?start=access_panel`; press `Bot chat info` | `action=get_chat ok=true update_id=... chat_id=... chat_type=...` | manual targeted | Low: read-only chat metadata summary. |
| Edit flow | deploy webhook example, then `./scripts/remote_logs.sh` | webhook deploy env | `/start` or `?start=smoke`; press `Edit message`, then `Remove keyboard` | `action=answer_callback_query`, `action=edit_message_text`, `action=edit_message_reply_markup` | manual targeted | Low: edits test message created by the bot. |
| Caption flow generated document | deploy webhook example, then `./scripts/remote_logs.sh` | webhook deploy env; media env optional | `/start`; press `Caption demo`, `Edit caption`, `Delete media message` | `action=send_media_caption_demo source=generated_document` or `file_id`/`media_path`, `action=edit_message_caption`, `action=delete_message` | manual targeted | Low/medium: uploads a small generated document or configured test media and deletes that test message. |
| Delete flow | deploy webhook example, then `./scripts/remote_logs.sh` | webhook deploy env | `/start`; press `Delete this message` | `action=delete_message ok=true update_id=... chat_id=... message_id=...` | manual targeted | Medium: deletes only the bot-created smoke message. |
| Reply flow | deploy webhook example, then `./scripts/remote_logs.sh` | webhook deploy env | Send a regular text message to the webhook bot | `action=send_chat_action`, `action=send_message reply_to_message_id=...` | manual targeted | Low: replies to the user's test message without logging full text. |
| Forward/copy flow | deploy webhook example, then `./scripts/remote_logs.sh` | webhook deploy env | `/start`; press `Copy this message`, then `Forward this message` | `action=copy_message copied_message_id=...`, `action=forward_message forwarded_message_id=...` | manual targeted | Low/medium: duplicates bot-created smoke messages in the same chat. |
| SendChatAction flow | deploy webhook example, then `./scripts/remote_logs.sh` | webhook deploy env | Send a regular text message | `action=send_chat_action ok=true chat_action=typing`, followed by `action=send_message` | manual targeted | Low. |
| v0.2 send methods smoke | `./scripts/smoke_v02_send_methods.sh` | main-role token, `AIGRAM_V02_SMOKE_CHAT_ID` or `AIGRAM_CHAT_ID`; optional media env for sticker/animation/video note | none | `AIGRAM_V02_SMOKE_SEND_CONTACT_OK`, `AIGRAM_V02_SMOKE_SEND_LOCATION_OK`, `AIGRAM_V02_SMOKE_SEND_VENUE_OK`, `AIGRAM_V02_SMOKE_SEND_POLL_OK`, `AIGRAM_V02_SMOKE_STOP_POLL_OK`, `AIGRAM_V02_SMOKE_SEND_DICE_OK`, optional media OK/SKIPPED markers, and `AIGRAM_V02_SMOKE_OK` | manual script, no user action | Low/medium: sends fixed test contact/location/venue/poll/dice messages and optional configured media to the smoke chat. Advanced Poll 9.6 fields remain manual-only unless a targeted smoke is added. |
| SendMediaGroup smoke | `./scripts/smoke_media_group.sh` | main-role token, `AIGRAM_MEDIA_GROUP_CHAT_ID` or `AIGRAM_CHAT_ID`; optional `AIGRAM_MEDIA_GROUP_FILE_ID_1/2` or `AIGRAM_MEDIA_GROUP_PATH_1/2` | none | `AIGRAM_MEDIA_GROUP_SMOKE_WAITING`, one of `AIGRAM_MEDIA_GROUP_FILE_ID_OK`/`AIGRAM_MEDIA_GROUP_UPLOAD_OK`/`AIGRAM_MEDIA_GROUP_GENERATED_UPLOAD_OK`, and `AIGRAM_MEDIA_GROUP_OK` | manual script, no user action | Low/medium: default generated-upload mode sends two small test documents as a media group; optional file IDs/paths are redacted from output. |

## Dangerous/admin flows

| Flow | Command/script | Required env | User action | Expected safe logs | Auto/manual | Risk |
| --- | --- | --- | --- | --- | --- | --- |
| Pin/unpin | no default script | Dedicated group/channel, bot admin rights | Run a dedicated probe only after explicit approval | Bot API result logs without private text | manual only | Admin/noisy; may alter chat pinned messages. |
| Moderation ban/restrict | no default script | Dedicated test group, bot admin rights, test target user, explicit confirmation | Run a dedicated probe; restore target with `UnbanChatMember` when needed | Method/result logs without admin list or private content | manual only | Destructive: can remove or restrict a user. Never auto-smoke. |
| Invite link methods | no default script | Dedicated test group/channel, bot admin rights with invite-user permission, explicit confirmation | Create/edit/revoke only test invite links; revoke created links after testing | Future smoke should log method names, redacted link metadata, and result flags only | manual only | Admin/state-changing: creates or revokes real access links. Never auto-smoke. |
| Chat join requests | no default script | Dedicated test group/channel, bot admin rights with invite-user permission, invite link with `creates_join_request=true`, explicit confirmation | A test account requests to join; approve or decline only that test request | Future smoke should log method names, chat ID, target user ID, and boolean result only | manual only | Admin/state-changing: approves or declines real pending users. Never auto-run. |
| Chat management methods | no default script | Dedicated test group/supergroup, bot admin rights with change-info and sticker-set permissions where applicable, explicit confirmation | Change only test title/description/photo/sticker set and revert them; test `LeaveChat` only with a disposable group/bot | Future smoke should log method names, chat ID, boolean result, and redacted file metadata only | manual only | Admin/state-changing: changes real chat profile/state or makes the bot leave. Never auto-run. |
| Forum topic methods | no default script | Dedicated test forum supergroup, bot admin rights with topic-management permissions, explicit confirmation | Create, edit, close, reopen, delete, and unpin only test topics; restore General topic state if changed | Future smoke should log method names, chat ID, message thread ID, and boolean result only | manual only | Admin/state-changing: changes real forum topic state. Never auto-run. |
| Reactions | no default script | Dedicated test chat and message, explicit confirmation | Set and clear reactions only on test messages | Future smoke should log method names, chat ID, message ID, reaction type labels, and boolean result only | manual only | State-changing: changes real message reaction state. Never auto-run. |
| Batch message methods | no default script | Dedicated test chat, disposable test messages, explicit confirmation | Forward/copy only test messages; delete only disposable messages created for the check | Future smoke should log method names, chat ID, message IDs count, first returned message ID, and boolean result only | manual only | `DeleteMessages` is destructive. Never auto-run batch delete. |
| Bot profile and metadata methods | no default script | Dedicated test bot, explicit confirmation, saved previous profile metadata | Read current name/description/short description/admin rights; set only temporary test metadata; restore previous values and profile photo after testing | Future smoke should log method names, language code, boolean result, and metadata field names only | manual only | Bot-state-changing: modifies real bot profile/metadata. Never auto-run. |
| Sticker set management | no default script | Dedicated test user/bot, disposable test sticker set, explicit confirmation | Create/add/replace/move/edit/delete only test sticker sets; clean up or delete the test set after testing | Future smoke should log method names, redacted sticker set names, file metadata without paths, and boolean results only | manual only | State-changing: mutates real sticker sets and uploaded sticker assets. Never auto-run. |
| Inline mode basics | no default script | Dedicated test bot with inline mode enabled in BotFather, dedicated test chat, explicit confirmation | Send an `@bot query` and choose only test article/location/venue/contact/game/media/cached media inline results | Future smoke should log update IDs, inline query IDs in redacted form, result types/counts, chosen result IDs, and boolean result only | manual only | Requires BotFather inline mode and sends inline results into a real chat. Never auto-run. |
| Payments, paid media, and Stars | no default script | Dedicated payment test bot/environment, provider or Stars test setup, explicit confirmation | Send only test invoices/paid media; answer only matching test `shipping_query` and `pre_checkout_query` updates; refund only matching test Stars payments | Future smoke should log method names, redacted query IDs/charge IDs, currency, amount/star count, media count, boolean result, and message ID/link presence only | manual only | Payment-state-changing: requires payment setup and can create real invoices, paid media, or refunds. Never auto-run. |
| Managed Bots 9.6 | no default script | Dedicated test manager bot with managed-bot capability, dedicated test managed bot/user, explicit confirmation | Save only test prepared keyboard buttons; fetch or replace only test managed bot tokens; never print returned tokens | Future smoke should log method names, user IDs, prepared button ID, boolean/link-free status, and token presence only | manual only | Sensitive: token-returning methods expose managed bot tokens and must never be auto-run or logged. |
| Advanced Poll 9.6 fields | no default script | Dedicated test chat, disposable poll messages, explicit confirmation | Send only test polls using `correct_option_ids`, revoting/options controls, descriptions, and poll-option replies; inspect only safe update metadata | Future smoke should log method names, poll ID in redacted form, option IDs/counts, and boolean result only | manual only | Poll-state-changing/noisy: can create visible polls and vote/update events. Never auto-run advanced poll checks. |
| Admin management methods | no default script | Dedicated test group, bot owner/admin rights, test user, explicit confirmation | Promote/demote only a test user; set custom title/default chat permissions only in the test group; revert changes after testing | Future smoke should log method names, chat ID, target user ID when present, and boolean result only | manual only | Admin/state-changing: changes real administrator or default chat permissions. Never auto-run. |
| Destructive methods | method-specific | Explicit confirmation and isolated test data | Depends on method | Safe action/result logs only | manual only | Includes `DeleteWebhook` with dropped pending updates, mass unpin, future migration methods, payments/refunds, and admin changes. |

## Safety rules

- Do not print bot tokens, webhook secrets, token-bearing URLs, private keys, `.env.local`, or full private message text.
- Prefer role-specific test bot tokens so long polling, webhook, local Bot API, destructive, and notify flows do not interfere.
- Keep `AIGRAM_SMOKE_MODE=targeted` by default; use `full` only for deliberate full regression.
- Use deep-link notifications for manual user actions, and verify with safe logs.
- If a network/live check fails, diagnose local TUN/Xray, SSH target, tunnel, local Bot API server, webhook reachability, firewall/listeners, systemd logs, and `GetWebhookInfo` before changing library code.
